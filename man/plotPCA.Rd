% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plotPCA}
\alias{plotPCA}
\title{Principal Component Analysis (PCA) chemical space plot}
\usage{
plotPCA(
  data,
  variables = NULL,
  color_by = "None",
  label_by = "None",
  scale_data = TRUE,
  ellipse = TRUE
)
}
\arguments{
\item{data}{A data.frame with numeric property columns.}

\item{variables}{Character vector of numeric column names to use in the
PCA. If \code{NULL}, a default set of common descriptors is used.}

\item{color_by}{Character. Name of the column to colour points by, or
\code{"None"} for a single colour.}

\item{label_by}{Character. Name of the column to use as point labels, or
\code{"None"} for no labels.}

\item{scale_data}{Logical. Whether to scale variables to unit variance
before the PCA. Default \code{TRUE}.}

\item{ellipse}{Logical. Whether to draw 95\% confidence ellipses per group.
Default \code{TRUE}.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Computes a PCA on the selected numeric physicochemical variables and
produces a scatterplot of the first two principal components, optionally
coloured by a grouping variable, with 95\% confidence ellipses, observation
labels and variable loading arrows. Labels require the suggested package
\pkg{ggrepel}.
}
\examples{
\dontrun{
d <- data.frame(MW = c(300, 400, 500), LogP = c(2, 3, 4),
  TPSA = c(60, 80, 100), group = c("A", "A", "B"))
plotPCA(d, variables = c("MW", "LogP", "TPSA"), color_by = "group")
}
}
